/*:#localized(key: "FirstProseBlock")
 **Goal**: Define a function to execute your commands.
 
 Now, we’ll create a **function** which will allow us to perform a sequence of movements with just one command.
 
 **Instructions:**
 1. Tap `funcName` and enter a name for your function
 2. Tap the area between the function's `{` and `}` braces and enter **at least two commands**, such as `moveForward()`, `moveBackward()`, `turnLeft()`, `turnRight()`, or `wait()`
 3. Below the function, enter your function call. This will be the name of your function followed by `()`
 4. Tap the **Run My Code** button
 
 */
//#-code-completion(everything, hide)
//#-code-completion(identifier, show, moveForward(), turnLeft(), moveBack(), turnRight(), wait())
//#-hidden-code
setup()
//#-end-hidden-code
func /*#-editable-code yourFuncName*/<#funcName#>/*#-end-editable-code*/() {
    //#-hidden-code
    customFunctionCalled()
    //#-end-hidden-code
    //#-editable-code
    //#-end-editable-code
}
//#-editable-code Call your function
//#-editable-code
//#-end-editable-code
//#-hidden-code
exitProgram()
//#-end-hidden-code


